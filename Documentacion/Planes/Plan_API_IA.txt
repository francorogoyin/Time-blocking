Plan de implementacion para API de IA de Semaplan.

Fecha de redaccion inicial: 2026-04-16.
Fecha de actualizacion: 2026-07-03.

Objetivo.

Mantener una API B1 de lectura y sumar una B2 de escritura acotada
para que ChatGPT pueda operar Semaplan con autorizacion explicita del
usuario.

La API debe cubrir el estado operativo real de la app:

- agenda y eventos;
- tareas;
- habitos;
- slots vacios, tipos de slot y planes de slot;
- planes semanales;
- planes por periodos;
- Archivero;
- Baul;
- metas;
- contexto compacto.
- acciones B2 sobre tareas, habitos, metas, Archivero y Baul.

La API debe exponer vistas legibles y seguras, sin devolver el blob
crudo completo de `public.estado_usuario.estado`.

Este documento ya no describe solo la idea general. Debe servir como
guia viva para implementar, mantener y ampliar el gateway de IA:
contrato publico, normalizadores, acciones B2, tests y criterio de
cierre.

## Decision tecnica

Crear un Semaplan AI Gateway como capa unica de lectura y mutaciones
acotadas.

- Backend unico:
  `supabase/functions/semaplan-ai/index.ts`
- Consumo de ChatGPT B2:
  GPT Actions + OpenAPI + OAuth
- Consumo de ChatGPT Apps B1:
  MCP remoto de lectura
- Consumo de Claude:
  MCP remoto o tool use intermedio

No crear dos backends separados para GPT y Claude.

## Fuente real de datos

La fuente remota de verdad es `public.estado_usuario`.

La columna `estado` contiene el blob JSON completo del usuario.

La app operativa real hoy vive principalmente en `login.html`.
`index.html` es landing publica y `Semaplan.html` redirige a
`login.html`.

Antes de codificar un normalizador, relevar en `login.html` la
estructura actual del subsistema correspondiente.

Claves persistidas relevantes observadas hoy:

- `Objetivos`
- `Eventos`
- `Metas`
- `Slots_Muertos`
- `Planes_Slot`
- `Planes_Semana`
- `Planes_Periodo`
- `Plantillas_Subobjetivos`
- `Tareas`
- `Tareas_Cajones_Definidos`
- `Habitos`
- `Habitos_Registros`
- `Baul_Objetivos`
- `Archiveros`
- `Notas_Archivero`
- `Etiquetas_Archivero`
- `Patrones`
- `Categorias`
- `Etiquetas`
- `Tipos_Slot`
- `Slots_Muertos_Tipos`
- `Slots_Muertos_Nombres`
- `Slots_Muertos_Titulos_Visibles`
- `Config_Extra`
- `Inicio_Semana`

Si durante la implementacion aparece una clave adicional que sea
necesaria para exponer un dato visible, agregarla al normalizador sin
romper el contrato publico.

## Principios de version 1

La version 1 debe ser estrictamente read-only.

Permitido:

- leer estado normalizado por dominios;
- consultar por fecha, rango, cajon, estado o texto;
- devolver resumenes compactos para IA.

No permitido:

- crear, editar, mover o borrar datos;
- disparar sync;
- devolver el blob completo;
- devolver credenciales, tokens, emails de terceros o datos de debug;
- usar cookies;
- exponer service role key al frontend.

## Principios de B2

La B2 habilita escritura desde ChatGPT, pero no convierte al GPT en un
cliente libre sobre todo el blob de estado.

Permitido:

- crear tareas sueltas;
- marcar tareas como hechas o pendientes;
- reprogramar tareas no vinculadas a agenda o planes;
- crear habitos simples;
- registrar cumplimiento de habitos;
- registrar avances manuales de metas;
- crear notas en Archivero;
- crear items en Baul.

No permitido en B2:

- borrar datos;
- editar datos masivamente;
- mover notas, objetivos o items entre estructuras;
- modificar Discoteca o Decoteca;
- administrar tokens;
- cambiar agenda, eventos, planes vinculados o estructuras sensibles.

Reglas tecnicas de B2:

- cada dominio tiene scope propio;
- toda mutacion debe validar el scope requerido;
- cada request puede traer `idempotency_key`;
- cada mutacion aplicada se registra en `ia_mutaciones_usuario`;
- la escritura usa control optimista de `version` sobre
  `estado_usuario`;
- los errores deben devolver una razon accionable para que ChatGPT
  pida precision en vez de improvisar.

Scopes B2:

- `write_tasks`;
- `write_habits`;
- `write_metas`;
- `write_archivero`;
- `write_baul`.

Discoteca y Decoteca quedan fuera de B2 y se evaluan para B3.

## Autenticacion

La integracion usa tokens de IA por usuario, emitidos directamente o
por el flujo OAuth de ChatGPT.

Tabla nueva recomendada:
`public.tokens_ia_usuario`

Columnas recomendadas:

- `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
- `usuario_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`
- `nombre TEXT NOT NULL DEFAULT 'Integracion IA'`
- `token_hash TEXT NOT NULL UNIQUE`
- `scopes TEXT[] NOT NULL DEFAULT ARRAY['read']`
- `ultimo_uso_en TIMESTAMPTZ`
- `creado_en TIMESTAMPTZ NOT NULL DEFAULT NOW()`
- `revocado_en TIMESTAMPTZ`

Reglas:

- El token real se muestra una sola vez.
- La base guarda solo hash SHA-256.
- El cliente legacy envia `X-Semaplan-AI-Token`.
- ChatGPT Actions usa `Authorization: Bearer <token>` emitido por
  OAuth.
- La Edge Function hashea el token entrante y busca su hash.
- Si el token no existe o esta revocado, rechazar.
- Leer solo el estado del `usuario_id` asociado al token.
- Aceptar `Authorization: Bearer <jwt>` solo como modo interno de
  prueba para la app web.
- Para escribir, el token debe incluir el scope especifico del dominio.
- El scope `read` sigue siendo obligatorio en toda autorizacion OAuth.

## Base URL

Base publica:
`https://<project>.supabase.co/functions/v1/semaplan-ai`

Todos los endpoints deben vivir bajo la misma Edge Function.

## Contrato publico de v1

### 1. `GET /salud`

Uso:
chequeo simple del servicio.

Respuesta:

```json
{
  "Ok": true,
  "Servicio": "Semaplan AI Gateway",
  "Version": "2.0.0"
}
```

### 2. `GET /contexto?desde=YYYY-MM-DD&hasta=YYYY-MM-DD`

Uso:
endpoint recomendado para preguntas generales.

Rango maximo:
45 dias.

Respuesta minima esperada:

```json
{
  "Ok": true,
  "Contexto": {
    "Desde": "2026-04-27",
    "Hasta": "2026-05-03",
    "Agenda": {},
    "Tareas": {},
    "Habitos": {},
    "Slots": {},
    "Planes_Semana": {},
    "Planes_Periodo": {},
    "Archivero": {},
    "Baul": {},
    "Metas": {}
  }
}
```

No tiene que devolver listas completas pesadas si una cuenta alcanza.
Debe priorizar resumenes y proximos items.

### 3. `GET /agenda?desde=YYYY-MM-DD&hasta=YYYY-MM-DD`

Uso:
leer eventos calendarizados, slots muertos y planes de slot en rango.

Rango maximo:
45 dias.

Respuesta:

```json
{
  "Ok": true,
  "Desde": "2026-04-27",
  "Hasta": "2026-05-03",
  "Bloques": [
    {
      "Id": "Ev_1",
      "Fecha": "2026-04-27",
      "Inicio": "09:00",
      "Fin": "10:00",
      "Titulo": "Leer Foucault",
      "Tipo": "Evento",
      "Objetivo_Id": "Obj_1",
      "Objetivo_Nombre": "Lectura",
      "Categoria": "Estudio",
      "Nota": "Capitulo 1",
      "Estado": "Planeado",
      "Origen": "Calendario",
      "Plan_Slot": null
    }
  ]
}
```

### 4. `GET /tareas?desde=YYYY-MM-DD&hasta=YYYY-MM-DD&cajon=<texto>&estado=<texto>&limite=100`

Uso:
leer tareas sueltas por rango, cajon y estado.

Limites:

- `limite` default 50
- `limite` maximo 100
- rango maximo 45 dias cuando se usa rango

Respuesta:

```json
{
  "Ok": true,
  "Tareas": [
    {
      "Id": "Tarea_1",
      "Nombre": "Mandar mail",
      "Emoji": "•",
      "Cajon": "Inbox",
      "Prioridad": "Media",
      "Estado": "pendiente",
      "Fecha": "2026-04-27",
      "Hora": "11:00",
      "Planeada": true,
      "Evento_Id": "Ev_10",
      "Plan_Clave": "2026-04-27|11",
      "Fecha_Creacion": "2026-04-27T10:00:00.000Z",
      "Fecha_Actualizacion": "2026-04-27T10:10:00.000Z"
    }
  ]
}
```

### 5. `GET /habitos?fecha=YYYY-MM-DD&modo=Dia|Semana|Quincena|Mes&limite=100`

Uso:
leer habitos visibles para una fecha y su contexto operativo.

Respuesta:

```json
{
  "Ok": true,
  "Fecha": "2026-04-27",
  "Modo": "Semana",
  "Habitos": [
    {
      "Id": "Hab_1",
      "Nombre": "Caminar",
      "Emoji": "🚶",
      "Tipo": "Hacer",
      "Activo": true,
      "Archivado": false,
      "Fecha_Inicio": "2026-04-01",
      "Programacion": {},
      "Meta": {},
      "Estado_Visible": "Pendiente",
      "Ultimo_Registro": null
    }
  ]
}
```

### 6. `GET /slots?desde=YYYY-MM-DD&hasta=YYYY-MM-DD&limite=200`

Uso:
leer slots vacios, tipos, nombres visibles y planes de slot.

Respuesta:

```json
{
  "Ok": true,
  "Slots": [
    {
      "Fecha": "2026-04-27",
      "Hora": "15:00",
      "Tipo_Id": "Slot_Trabajo",
      "Tipo_Nombre": "Trabajo",
      "Titulo_Visible": "Bloque libre",
      "Plan": {
        "Items": [],
        "Nota": ""
      }
    }
  ]
}
```

### 7. `GET /planes/semana?semana=YYYY-MM-DD`

Uso:
leer el snapshot de plan semanal y su diff contra eventos actuales.

Respuesta:

```json
{
  "Ok": true,
  "Plan_Semana": {
    "Semana": "2026-04-27",
    "Fijada_En": "2026-04-27T03:00:00.000Z",
    "Cerrada_En": null,
    "Nota_Inicial": "",
    "Nota_Cierre": "",
    "Veces_Refijada": 0,
    "Eventos_Base": [],
    "Diff": {
      "Agregados": [],
      "Quitados": [],
      "Movidos": [],
      "Duracion": []
    }
  }
}
```

### 8. `GET /planes/periodos?periodo_id=<id>&tipo=<texto>&limite=100`

Uso:
leer el sistema jerarquico de planes por periodos.

Reglas:

- si no viene `periodo_id`, devolver lista de periodos y resumen;
- si viene `periodo_id`, devolver periodo y su arbol relevante.

Respuesta:

```json
{
  "Ok": true,
  "Periodo": {},
  "Objetivos": [],
  "Subobjetivos": [],
  "Partes": [],
  "Avances": []
}
```

### 9. `GET /archivero?cajon_id=<id>&limite=50`

Uso:
leer notas de un cajon.

Reglas:

- si no viene `cajon_id`, usar cajon seleccionado o primer cajon;
- `limite` default 50;
- `limite` maximo 100.

Respuesta:

```json
{
  "Ok": true,
  "Cajon": {
    "Id": "Arch_1",
    "Nombre": "Semaplan"
  },
  "Notas": [
    {
      "Id": "Nota_1",
      "Titulo": "Idea",
      "Texto": "Texto",
      "Etiquetas": ["Inmediata"],
      "Origen": "",
      "Fecha_Creacion": 0,
      "Fecha_Actualizacion": 0
    }
  ]
}
```

### 10. `GET /archivero/buscar?q=<texto>&limite=20`

Uso:
buscar en titulo, texto, etiquetas y origen.

Reglas:

- normalizar mayusculas, acentos y espacios;
- `q` maximo 200 caracteres;
- `limite` default 20;
- `limite` maximo 50.

### 11. `GET /buscar`

Uso:
buscar transversalmente o consultar Decoteca por teca, filtros y lotes.

Para Decoteca usar `modulo=decoteca` y, opcionalmente, `teca_id`.
`limite` acepta hasta 100 resultados y `offset` indica cuántos resultados
ordenados se deben saltar; si se omite, vale 0. El orden de Decoteca es
estable por `Orden` y luego `Id`.

Filtros explícitos disponibles:

- `descripcion_vacia`, `creador_vacio`, `anio_vacio`;
- `genero_vacio`, `subgenero_vacio`, `portada_vacia`;
- `total_unidades_cero`, `estado`, `periodo` y `prioridad`.

Para respuestas pequeñas usar `compacto=true`. La respuesta de Decoteca
incluye `Total`, `Offset`, `Limite`, `Hay_Mas` y `Siguiente_Offset`.

### 12. `GET /baul?categoria=<texto>&estado=<texto>&limite=100`

Uso:
leer objetivos del Baul.

Respuesta:

```json
{
  "Ok": true,
  "Objetivos": [
    {
      "Id": "Baul_1",
      "Nombre": "Curso X",
      "Emoji": "📚",
      "Estado": "Activo",
      "Archivada": false,
      "Categoria": "Estudio",
      "Etiquetas": [],
      "Descripcion": "",
      "Timeline": null,
      "Metadatos": {}
    }
  ]
}
```

### 13. `GET /metas?limite=50`

Uso:
leer metas visibles y resumidas.

Respuesta:

```json
{
  "Ok": true,
  "Metas": [
    {
      "Id": "Meta_1",
      "Nombre": "Lectura",
      "Periodo": "Semana",
      "Unidad": "Horas",
      "Objetivo": 8,
      "Progreso": 3,
      "Estado": "En_Ritmo",
      "Detalle": null
    }
  ]
}
```

### 14. `GET /openapi.json`

Uso:
servir esquema OpenAPI minimo para GPT Actions.

Puede estar en runtime o generarse desde archivo del repo.

## Contrato publico de B2

Todos los endpoints B2 usan `POST`, OAuth y JSON. El token debe tener
`read` y el scope de escritura correspondiente.

### 1. `POST /b2/tareas/crear`

Scope:
`write_tasks`.

Payload minimo:

```json
{
  "nombre": "Mandar mail",
  "fecha": "2026-07-03",
  "hora": "11:00",
  "cajon": "Inbox",
  "idempotency_key": "crear-tarea-mail-2026-07-03"
}
```

### 2. `POST /b2/tareas/marcar`

Scope:
`write_tasks`.

Payload minimo:

```json
{
  "tarea_id": "Tarea_1",
  "hecha": true,
  "idempotency_key": "marcar-tarea-1-hecha"
}
```

Tambien acepta `busqueda` o `nombre` cuando no se conoce el id, pero
si hay varias coincidencias debe fallar y pedir precision.

### 3. `POST /b2/tareas/reprogramar`

Scope:
`write_tasks`.

Payload minimo:

```json
{
  "tarea_id": "Tarea_1",
  "fecha": "2026-07-04",
  "hora": "15:30",
  "idempotency_key": "reprogramar-tarea-1-2026-07-04-1530"
}
```

No reprograma tareas vinculadas a agenda o planes.

### 4. `POST /b2/habitos/crear`

Scope:
`write_habits`.

Payload minimo:

```json
{
  "nombre": "Caminar",
  "tipo": "Hacer",
  "cantidad": 1,
  "idempotency_key": "crear-habito-caminar"
}
```

### 5. `POST /b2/habitos/registrar`

Scope:
`write_habits`.

Payload minimo:

```json
{
  "habito_id": "Habito_1",
  "fecha": "2026-07-03",
  "cantidad": 1,
  "idempotency_key": "registrar-habito-1-2026-07-03"
}
```

### 6. `POST /b2/metas/avance`

Scope:
`write_metas`.

Payload minimo:

```json
{
  "busqueda": "Lectura",
  "cantidad": 30,
  "unidad": "paginas",
  "fecha": "2026-07-03",
  "idempotency_key": "avance-lectura-2026-07-03"
}
```

### 7. `POST /b2/archivero/nota`

Scope:
`write_archivero`.

Payload minimo:

```json
{
  "texto": "Idea para revisar despues.",
  "titulo": "Idea",
  "cajon": "Ideas",
  "etiquetas": ["ChatGPT"],
  "idempotency_key": "nota-idea-2026-07-03"
}
```

### 8. `POST /b2/baul/item`

Scope:
`write_baul`.

Payload minimo:

```json
{
  "nombre": "Curso de fotografia",
  "descripcion": "Revisar opciones.",
  "estado": "activo",
  "idempotency_key": "baul-curso-fotografia"
}
```

## Reglas de normalizacion por dominio

### Agenda

- inspeccionar `login.html` para mapear eventos reales;
- horarios en `America/Buenos_Aires`;
- omitir eventos sin fecha valida;
- incluir nombre de objetivo asociado si existe;
- incluir nota solo si es breve;
- si una nota supera 1000 caracteres, truncar y marcar
  `Texto_Truncado: true`.

### Tareas

- no mezclar tareas con eventos aunque puedan estar vinculadas;
- si la tarea esta pospuesta, devolver fecha y hora vacias;
- exponer solo referencias minimas a slot, plan o evento;
- no exponer borradores ni flags solo de UI.

### Habitos

- devolver solo habitos visibles para fecha y modo pedido;
- incluir programacion y meta en formato legible;
- resumir registros relevantes sin bajar historial entero pesado;
- no exponer filtros ni estados internos de UI.

### Slots

- incluir `Fecha`, `Hora`, `Tipo_Id`, `Tipo_Nombre`,
  `Titulo_Visible` y plan explicito si existe;
- si un slot tiene items planeados, devolver lista corta legible;
- no duplicar metadata pesada por cada entrada si puede resumirse.

### Planes semanales

- exponer snapshot base y diff resumido;
- no bajar toda la semana completa dentro de `/contexto`;
- si la semana no existe, devolver estructura vacia consistente.

### Planes por periodos

- devolver periodos, objetivos, subobjetivos y partes con:
  `Id`, `Nombre`, `Estado`, `Progreso`, `Unidad`,
  fechas y relaciones padre-hijo;
- resumir avances;
- no exponer configuracion `UI` salvo lo minimo necesario.

### Archivero

- ordenar notas por fecha mas nueva primero;
- buscar por texto normalizado;
- si no hay titulo, `Titulo: null`;
- si no hay etiquetas, `Etiquetas: []`;
- no devolver color de fondo salvo necesidad concreta;
- si hay adjuntos, no devolver base64 completo en v1.

### Baul

- devolver objetivos accionables y archivados visibles;
- incluir categoria legible;
- incluir timeline si existe;
- resumir metadatos visibles;
- no devolver estructuras duplicadas pesadas.

### Metas

- devolver nombre, periodo, unidad, objetivo, progreso y estado;
- si no se puede inferir progreso con seguridad, usar `Detalle`;
- agregar test antes de publicar si la estructura real queda ambigua.

## Estructura interna recomendada de la Edge Function

Archivo principal:
`supabase/functions/semaplan-ai/index.ts`

Funciones internas sugeridas:

- `Responder_Json`
- `Responder_Error`
- `Validar_Request`
- `Leer_Estado_Usuario`
- `Normalizar_Texto_Busqueda`
- `Parsear_Fecha`
- `Limitar_Rango_Fechas`
- `Normalizar_Contexto`
- `Normalizar_Agenda`
- `Normalizar_Tareas`
- `Normalizar_Habitos`
- `Normalizar_Slots`
- `Normalizar_Planes_Semana`
- `Normalizar_Planes_Periodo`
- `Normalizar_Archivero`
- `Normalizar_Baul`
- `Normalizar_Metas`
- `Filtrar_Campos_Seguros`

Si el archivo principal crece demasiado, separar helpers por dominio
sin cambiar el contrato del endpoint.

## CORS, errores y limites

CORS:

- responder siempre `OPTIONS`;
- permitir `Authorization`, `apikey`, `content-type` y
  `X-Semaplan-AI-Token`;
- usar `Access-Control-Allow-Origin: *` solo sin cookies.

Formato de error:

```json
{
  "Ok": false,
  "Error": "No autorizado",
  "Detalle": "Token invalido."
}
```

Codigos HTTP:

- `200` exito
- `400` parametros invalidos
- `401` sin token o JWT invalido
- `403` token revocado o scope insuficiente
- `404` ruta inexistente
- `429` rate limit
- `500` error inesperado

Rate limit minimo de v1:

- 60 requests por minuto por token
- 500 requests por dia por token

Si no entra tabla de rate limit en el primer commit tecnico, dejar el
codigo preparado y documentar deuda explicita. No cerrar la API
publica sin rate limit.

## Archivos a crear

- `supabase/functions/semaplan-ai/index.ts`
- `supabase/migrations/YYYYMMDD_tokens_ia_usuario.sql`
- `Documentacion/Planes/OpenAPI_Semaplan_AI.yaml`
- `Documentacion/Planes/Instrucciones_GPT_Semaplan_AI.txt`
- `Documentacion/Planes/Instrucciones_Claude_MCP_Semaplan_AI.txt`

Opcional si entra MCP en el mismo turno:

- `Herramientas/Scripts/semaplan-ai-mcp-server.js`

## Fases de implementacion

### Fase 0. Cerrar contrato

Objetivo:
definir payloads, reglas de normalizacion y limites antes de escribir
la funcion.

Salida esperada:

- este documento cerrado;
- OpenAPI bosquejado;
- decision tomada sobre campos minimos por endpoint.

### Fase 1. Autenticacion y esqueleto

Archivos:

- migration `tokens_ia_usuario`
- Edge Function con `OPTIONS`, `/salud` y validacion de token

Tests minimos:

- `/salud` responde `200`
- request sin token responde `401`
- token revocado responde `403`

Commit recomendado:
`Agregar autenticacion base de semaplan-ai`

### Fase 2. Lectura segura del estado

Objetivo:
leer `estado_usuario` del usuario autenticado y filtrar campos seguros.

Tests minimos:

- no mezclar usuarios
- no devolver blob crudo completo
- no exponer tokens ni credenciales

Commit recomendado:
`Agregar lectura segura de estado para semaplan-ai`

### Fase 3. Contexto y agenda

Endpoints:

- `/contexto`
- `/agenda`

Tests minimos:

- rango > 45 dias da `400`
- evento sin fecha valida se omite
- agenda devuelve nombre de objetivo asociado si existe

Commit recomendado:
`Agregar contexto y agenda a semaplan-ai`

### Fase 4. Operacion diaria

Endpoints:

- `/tareas`
- `/habitos`
- `/slots`

Tests minimos:

- `/tareas` devuelve solo tareas normalizadas
- tarea pospuesta sale sin fecha ni hora
- `/habitos` devuelve estado visible consistente
- `/slots` devuelve tipo, titulo visible y plan si existe

Commit recomendado:
`Agregar tareas habitos y slots a semaplan-ai`

### Fase 5. Planificacion

Endpoints:

- `/planes/semana`
- `/planes/periodos`

Tests minimos:

- `/planes/semana` devuelve diff resumido
- `/planes/periodos` expone objetivos, subobjetivos y partes
- no exponer estructuras internas de UI innecesarias

Commit recomendado:
`Agregar planes semanales y por periodos a semaplan-ai`

### Fase 6. Memoria y backlog

Endpoints:

- `/archivero`
- `/archivero/buscar`
- `/baul`
- `/metas`

Tests minimos:

- `/archivero/buscar` no depende de acentos
- Archivero ordena mas nuevo primero
- Baul devuelve categoria legible
- Metas devuelven estado y progreso o detalle seguro

Commit recomendado:
`Agregar archivero baul y metas a semaplan-ai`

### Fase 7. OpenAPI y GPT

Archivos:

- `OpenAPI_Semaplan_AI.yaml`
- `Instrucciones_GPT_Semaplan_AI.txt`

Tests minimos:

- OpenAPI contiene todos los endpoints de v1
- auth declarada por OAuth

Commit recomendado:
`Agregar OpenAPI e instrucciones GPT para semaplan-ai`

### Fase 8. Claude MCP o camino intermedio

Archivos:

- `Instrucciones_Claude_MCP_Semaplan_AI.txt`
- MCP remoto opcional en este turno

Tests minimos:

- todas las herramientas MCP mapear al gateway
- MCP no leer Supabase directo

Commit recomendado:
`Documentar integracion Claude para semaplan-ai`

### Fase 9. UI minima de tokens

Objetivo:
seccion `Integraciones IA` en Configuracion.

Botones:

- `Crear token de lectura`
- `Copiar token`
- `Revocar token`

Tests minimos:

- token completo se muestra una sola vez
- token no queda en localStorage
- revocacion invalida el uso posterior

Commit recomendado:
`Agregar gestion de tokens IA en configuracion`

### Fase 10. Validacion final

Validaciones obligatorias:

- `npm run test`
- `npm run test:smoke:prod`
- deploy o comando pendiente documentado

Si el deploy no puede ejecutarse por credenciales, dejar el cambio
listo y anotar exactamente el comando pendiente.

## Matriz minima de pruebas por endpoint

- `/salud`: responde `200` y version
- `/contexto`: respeta rango y no devuelve payload excesivo
- `/agenda`: valida rango y omite eventos invalidos
- `/tareas`: filtra tareas y no mezcla objetivos
- `/habitos`: calcula estado visible segun fecha y modo
- `/slots`: devuelve plan de slot y tipo visible
- `/planes/semana`: devuelve snapshot y diff
- `/planes/periodos`: devuelve jerarquia segura
- `/archivero`: ordena y no baja base64 de adjuntos
- `/archivero/buscar`: normaliza acentos
- `/baul`: devuelve categoria y timeline si existe
- `/metas`: devuelve progreso o detalle seguro
- `/openapi.json`: refleja endpoints y auth real

## Integracion con GPT

Crear Custom GPT `Semaplan`.

Para B2, usar GPT Actions con:

- OpenAPI de
  `Documentacion/Planes/OpenAPI_Semaplan_AI.yaml`
- auth OAuth
- scope completo:
  `read write_tasks write_habits write_metas write_archivero write_baul`

Instrucciones base del GPT:

- responder solo cuando el usuario pida datos de Semaplan;
- no inventar horarios, notas, tareas, habitos ni objetivos;
- pedir rango de fechas si la pregunta temporal es ambigua;
- si el endpoint devuelve vacio, decirlo;
- escribir solo cuando el usuario pida una accion concreta;
- no borrar datos ni operar Discoteca o Decoteca en B2.

Para B1 de lectura en ChatGPT Apps, mantener el MCP remoto
`semaplan-ai-mcp`.

## Integracion con Claude

Opcion recomendada:
MCP remoto.

Herramientas esperadas:

- `semaplan_contexto`
- `semaplan_agenda`
- `semaplan_tareas`
- `semaplan_habitos`
- `semaplan_slots`
- `semaplan_plan_semana`
- `semaplan_planes_periodos`
- `semaplan_buscar_archivero`
- `semaplan_listar_archivero`
- `semaplan_listar_baul`
- `semaplan_listar_metas`

Reglas:

- todas deben llamar al gateway;
- MCP no debe leer Supabase directo;
- reenviar el token como `X-Semaplan-AI-Token`.

## Seguridad

- no usar endpoints sin autenticacion;
- no loguear contenido de notas, agenda, tareas ni habitos;
- no devolver stack traces al cliente;
- no aceptar queries de busqueda > 200 caracteres;
- no aceptar rangos temporales abiertos o ilimitados;
- no mezclar datos entre usuarios;
- no guardar tokens reales en base ni localStorage.

## Escritura B2 y escritura futura

La escritura B2 ya existe para tareas, habitos, metas, Archivero y
Baul.

Scopes activos:

- `read`;
- `write_tasks`;
- `write_habits`;
- `write_metas`;
- `write_archivero`;
- `write_baul`.

La B2 requiere:

- pedido explicito del usuario;
- scopes separados;
- auditoria;
- idempotencia;
- rechazo de acciones ambiguas;
- control optimista de version.

Queda fuera para versiones futuras:

- borrado;
- edicion masiva;
- escritura de agenda y planes vinculados;
- administracion de tokens;
- Discoteca y Decoteca;
- undo desde el GPT.

## Cierre de tarea

La tarea se considera cerrada cuando existan:

- API B1 de lectura segura;
- acciones B2 acotadas y auditadas;
- tokens OAuth por usuario con scopes separados;
- OpenAPI usable por GPT Actions;
- camino documentado o implementado para Claude;
- pruebas reportadas;
- deploy ejecutado o comando pendiente explicitado.

## Referencias oficiales

OpenAI GPT Actions:
`https://platform.openai.com/docs/actions/getting-started/getting-started`

OpenAI function calling:
`https://platform.openai.com/docs/guides/function-calling`

Anthropic MCP connector:
`https://docs.anthropic.com/en/docs/agents-and-tools/mcp-connector`

Anthropic tool use:
`https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview`

## Nota final

La secuencia recomendada es esta:

1. cerrar contrato;
2. implementar lectura segura;
3. abrir GPT Actions;
4. abrir Claude MCP;
5. recien despues evaluar escritura.
